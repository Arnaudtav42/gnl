#include "get_next_line.h"
/*
int ft_cpydecalagebuff(char **line, const char buffer, int count)
{
	if(*line)
	{
		*line = ft_strnjoin(line, buffer, count);
		if (!line)
			return(-1);
		ft_strncpy(buffer, buffer + count + 1, BUFFER_SIZE - count);
		return 1;
	}	
}	*/ 	